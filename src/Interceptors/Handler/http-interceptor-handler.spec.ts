import { HttpInterceptorHandler } from './http-interceptor-handler';

describe('HttpInterceptorHandler', () => {
  it('should create an instance', () => {
    expect(new HttpInterceptorHandler()).toBeTruthy();
  });
});
