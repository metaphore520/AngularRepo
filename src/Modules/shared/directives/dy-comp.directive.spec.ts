import { DyCompDirective } from './dy-comp.directive';

describe('DyCompDirective', () => {
  it('should create an instance', () => {
    const directive = new DyCompDirective();
    expect(directive).toBeTruthy();
  });
});
