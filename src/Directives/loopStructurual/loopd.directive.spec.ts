import { LoopdDirective } from './loopd.directive';

describe('LoopdDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopdDirective();
    expect(directive).toBeTruthy();
  });
});
